<link rel="stylesheet" href="/forms.css">
<link rel="stylesheet" href="/card.css">
<link rel="stylesheet" href="/add-tournament-page.css">

<main>
    <div class="card">
        <h1>add tournament</h1>
        
        <form method="post" id="form">

            <div class="splitCols">
                <div class="left">
                    <label>Name<input type="text" name="name" required></label>
                    <label>Players order
                        <select name="crit">
                            <option value="name">Name</option>
                            <option value="rating" selected>Rating</option>
                            <option value="age">Age</option>
                        </select>
                    </label>
                </div>
                
                <div class="right">
                    <label>Players
                        <label>
                            <input type="checkbox" id="players-all">
                            Wszyscy gracze
                        </label>
                
                        <div class="scrDiv">
                            {{#each users}}
                                {{#unless this.deleted}}{{#unless (eq this._id -1)}}
                                    <div>
                                        <label class="cb">
                                            <input type="checkbox" name="p-{{this._id}}" class="player-cb">
                                            <a href="/players/{{this._id}}" class="no-style">{{this.name}} {{this.surname}}</a>
                                        </label>
                                    </div>
                                {{/unless}}{{/unless}}
                            {{/each}}
                        </div>
                    </label>
                    <i id="error"></i>
                </div>
            </div>
        
            <div class="btns">
                <input class="accept btn" type="submit" value="add">
                <button class="cancel btn" onclick="goBack()">cancel</button>
            </div>
        </form>
    </div>
</main>