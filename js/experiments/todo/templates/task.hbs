<div id="task">
    <h2>{{task.name}} - #{{task.id}}</h2>
    <i>created {{task.date}}s ago</i>
    
    <p>{{task.desc}}</p>
    
    <a href="/">go back</a>
    <a href="/delete/{{task.id}}" class="danger">remove</a>
</div>